# Проверка организационных изменений

Проект HackAlem: сравнение подразделений и функций до/после реорганизации с открываемыми источниками.

**Состояние:** реализованы локальный сервер, парсеры, агентный цикл и технический интерфейс. Локальные тесты используют подмену API модели; полный анализ реальной моделью ещё не проверен. Ключ проекта прошёл отдельный короткий запрос. Промокредит не подтверждён. Интерфейс в `frontend/` разрабатывает второй участник.

## Запуск

Нужны Git, [uv](https://docs.astral.sh/uv/getting-started/installation/) и собственный OpenAI API key. Проверено на Windows с Python 3.12.11.

```sh
git clone https://github.com/BAITC-Hacks/hack-1086dac4-amazing-guys.git
cd hack-1086dac4-amazing-guys
uv sync --project backend --locked --python 3.12
```

Скопируйте `.env.example` в `.env` в корне проекта и впишите `OPENAI_API_KEY`. Существующий `.env` не перезаписывайте. Сервер читает ключ только из этого файла, игнорируя одноимённую переменную окружения процесса. `.env` исключён из Git; ключ не передаётся браузеру.

```sh
uv run --project backend --locked uvicorn backend.app:app --host 127.0.0.1 --port 8000
```

Откройте http://127.0.0.1:8000. Документация: `/docs`; схема: `/openapi.json`; проверка конфигурации без запроса к модели: `/api/health`.

Выберите `fixtures/org-review/before.md` в «До», `after.md` в «После», нажмите начало анализа. **Это платные запросы API**, передающие извлечённый текст OpenAI. Проверьте промокредит в аккаунте заранее. `expected.json` — эталон для оценки, его не загружать модели. Комплект авторский, не данные организатора.

## Обработка

1. Парсеры читают DOCX, XLSX, текстовый PDF, TXT или MD с адресами абзацев, таблиц, страниц и ячеек.
2. Модель `gpt-6-luna` получает извлечённый комплект, выбирает инструменты `search_functions` и `read_evidence`, затем составляет структурированный отчёт.
3. Сервер проверяет существование ссылок, версии, соответствия и выполненный поиск перед возможной потерей. Структурная проверка не доказывает правильность смысла.
4. Матрица, замечания, заключение, события инструментов, расход и цитаты сохраняются в `outputs/analyses/`. Исходные бинарные файлы не сохраняются. Браузер открывает точную цитату через API.

Стек: FastAPI/Pydantic, OpenAI SDK, python-docx, openpyxl, pypdf. Версии закреплены в `backend/uv.lock`. Один процесс и локальный пользователь, файловое хранение. Запускать с одним worker. Перезапуск помечает незавершённые задания ошибкой; готовые отчёты доступны по ID. Новый анализ требует повторной загрузки файлов.

## Проверки и ограничения

```sh
uv run --project backend --locked python -m pytest backend/tests -q -p no:cacheprovider
```

Тесты проверяют созданные DOCX/XLSX/PDF, адреса и цитаты, HTTP, повторные запросы, восстановление состояния, ошибки и лимиты агента. Ключ и сеть не нужны. Это не измерение качества модели на случаях ТЗ. HTTP-примеры: `fixtures/api/`.

Лимиты: по 5 файлов каждой версии, 5 МиБ на файл, 60 000 символов цитат и контекста, 4 запроса модели, 8 вызовов инструментов, 180 секунд модельного анализа. Оценочный бюджет анализа $0.25 — локальная проверка по тарифам, не ограничитель счёта OpenAI. Неизвестный расход после сбоя не выдаётся за нулевой. Прежний `Idempotency-Key` с теми же файлами возвращает прежнее задание без нового анализа.

OCR, DOC/XLS, изображения и сложные вложенные структуры полностью не поддерживаются. Непрочитанные части отмечаются предупреждениями. Возможная потеря означает «назначение не найдено в предоставленных документах». Поиск лексический; выводы и полнота реестра функций требуют проверки человеком. Нет авторизации и изоляции пользователей: запуск локальный, не публичный сервер. Реальный комплект организатора и независимый запуск товарищем ещё предстоят.

## Командная работа

Общая ветка `main`: капитан — `backend/` и контракт, товарищ — `frontend/`. Получайте изменения команды с сохранением своей работы; не используйте force push. Localhost товарища — его машина: ему нужен собственный локальный запуск и конфигурация.

- [SPEC.md](SPEC.md) — объём, приёмка, архитектура.
- [API_CONTRACT.md](API_CONTRACT.md) — реализованный контракт r1.
- [TEAMMATE_TASK.md](TEAMMATE_TASK.md) — задание интерфейса.
- [docs/CASE_BRIEF.md](docs/CASE_BRIEF.md) — ТЗ.
- [docs/SERVICE_SELECTION.md](docs/SERVICE_SELECTION.md) — выбор компонентов.
- [HACKATHON_STATE.md](HACKATHON_STATE.md) — проверенное состояние.

Использованы ТЗ пользователя, включённый `hackalem-coach`, Codex для подготовки и реализации, перечисленные открытые библиотеки и OpenAI API. Проверочные документы авторские. Загружаемые тексты — данные, не инструкции агенту. Официальная сдача не выполнялась.
