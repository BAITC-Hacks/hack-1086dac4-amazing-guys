# Проверка общей версии — 2026-09-23

## Последние завершённые проверки

- Реальная пара DOCX, `d3c1da1a66204b6dbbe8bf9c5d089248` (v6, код `ea83805`): completed, HTTP и открытие источников в UI прошли. 341.548 с, 5 модельных вызовов, 406326 входных / 37065 выходных токенов, оценочно $0.0591651. 120 функций, 63 соответствия, 5 изменений подразделений. Строгая независимая оценка: **4/6 полностью и 2 частично**, ≥5/6 ещё не выполнено. Частично: новые подразделения без явного описания областей; изменённый адресат поручения без явного сохранения общей ответственности главного аудитора. Остальные четыре контрольных изменения отражены. Два possible_loss описывают одну гипотезу контроля сроков, поэтому не считаются двумя независимыми рисками. Утверждения об окончательной потере обязанности нет.
- Авторский комплект, `94883f76f6d4428f859613f129697a0c`, medium: completed, **5/5** сценариев вручную проверены (переименование, возможная потеря, дубль, конфликт исполнения/контроля, различие подготовки/утверждения). 4 вызова, 21849 входных / 6498 выходных токенов, $0.0054339. Время от POST 323.128 с включает очередь за реальным прогоном и не является задержкой одной модели.
- **Текущий v7 `6eea88ce9c0d466f8910f592ae3140b3`: completed, 6/6 полностью.** 500.852 с, 6 вызовов, 400852 входных / 54842 выходных токенов, $0.0675062; 212 функций, 100 соответствий, одно осторожное possible_loss без дубля. Исправление: гипотезы разделены по назначенным блокам, существенные условия не сокращаются ради одного предложения. Дословный фрагмент одной функции исправлен отдельным малым вызовом без перегенерации отчёта. Реальный HTTP → UI → источники → новая карточка «до/после» проверены без нового платного анализа через существующий idempotency key.

| Контрольное изменение | Строка итогового отчёта | Результат |
| --- | --- | --- |
| Новые подразделения и явные области работы | p1-m013b, p1-a013b; источники doc-002:e00161–e00163 | Полностью |
| Передача координации других контрольных исполнителей | p1-m014d | Полностью |
| Мониторинг корректирующих действий сохранён и расширен | p1-m013e | Полностью |
| ДККМ: качество, методология, обучение | p1-m015b, p1-m015d, p1-m015f | Полностью |
| §9.15: обязательный анализ становится возможным | p2-M-23 | Полностью |
| §9.37: адресат поручения изменён, ответственность главного аудитора сохранена | p2-M-25 | Полностью |

Ограничения v7: это ручная проверка шести заранее выбранных случаев, а не полнота всех изменений. Шесть источников промежуточного обзора не представлены в итоговой матрице/замечаниях; число источников не равно числу пропущенных изменений. Реестр неизменённых обязанностей не гарантирован. Возможная потеря — гипотеза для сотрудника. При объединении сохраняются оговорки частей отчёта; фраза второй части об отсутствии possible_loss относится к её блокам, а не ко всему отчёту. 98 backend-тестов и 35 Playwright-проверок прошли, включая новые карточки и локальные решения; production build успешен.

Повторная авторская регрессия на текущем backend: `3bd4aa44014b40be8236e6a0b43dc524`, completed, 66.974с, 4 вызова, 21327 входных / 5467 выходных токенов, $0.0048662. Сохранены все пять ожидаемых сценариев; замечания — possible_loss архива, дубль реестра поставщиков, конфликт исполнения и независимой проверки платежей. Подготовка и утверждение плана различаются, переименование регистрации не объявлено потерей.

Коммит товарища `175974d` (гостевой вход и состояния загрузки) объединён в `71c24eb` с сохранением обоих наборов изменений. После merge: **35 Playwright-тестов прошли**, production build успешен, реальный v7 отчёт и карточка цитат открыты через объединённый UI. Подпись кнопки экспорта приведена к фактическому формату JSON; оба состояния загрузки товарища сохранены.

Числа ниже — история конкретных предыдущих версий, не текущие показатели.


Интерфейс «Контур» из коммита `25e4184` подключён к FastAPI; дополнительно объединён коммит товарища `dd9cbbf` с навигацией до анализа. Визуальный стиль сохранён; интеграционное изменение frontend касается только предела ожидания (10 минут).

## Исправления

- Большие документы: лимит считается по исходному тексту (250 000 символов), а не по многократно скопированному контексту. Каждый ID стоит непосредственно рядом со своей цитатой и версией; порядок сохранён, ни один фрагмент не отбрасывается. Полный соседний контекст доступен через read_evidence и API источников. Отдельный словарь повторяющихся текстов убран: на живой проверке модель путала его индексы с порядком source ID.
- Локальные порты 5173/4173/3000 разрешены для localhost и 127.0.0.1. CORS_ORIGINS добавляет дополнительные адреса; ключ остаётся только на backend.
- OpenAPI описывает фактический error-конверт вместо стандартного detail, а также health и HTTP-коды ошибок.
- Отчёт с некорректными ссылками получает не более одной модельной коррекции и повторную проверку; если она неуспешна, отчёт не публикуется. Максимум 6 запросов модели (с учётом обзора различий длинных редакций) и оценочный бюджет $0.25 на анализ.
- Первое инструментальное действие модели — поиск по комплекту после для проверки назначения функции с неочевидным правопреемником. Последующие инструменты выбирает модель. Для длинной пары редакций этому предшествует отдельный структурированный обзор блоков различий.
- Колонтитул только с полем номера страницы не помечает весь документ как частично непрочитанный. Содержательный пропущенный текст по-прежнему вызывает предупреждение.
- Источники функций наследуются сопоставлениями и замечаниями по явно указанным связям, затем рекомендациями. Новые источники код не угадывает; исходные ошибочные или повторные ID не удаляет — их отклоняет проверка. Сборка отмечается в activity. Это передача происхождения данных, не подтверждение смысловой связи.
- Для строк подразделений добавлена узкая проверка: если точное название встречается в данной версии, нужен источник, в котором оно названо. Сообщение коррекции перечисляет проблемные строки и ID таких фрагментов. Проверка не распознаёт все склонения/сокращения и не доказывает корректность остальных цитат.
- Модельная схема ограничивает evidence_ids реальными адресами комплекта. Учитываются [лимиты Structured Outputs](https://developers.openai.com/api/docs/guides/structured-outputs): 1000 enum-значений в схеме и 15 000 символов для большого строкового enum. Обе предоставленные редакции помещаются (981 источник). При превышении ограничений используется обычная схема и тот же серверный валидатор, источники не отбрасываются. Публичный HTTP r1 не меняется.
- Внутреннее поле source_excerpt у функции должно дословно совпасть с фрагментом её цитируемого источника; соседний действительный ID с другим текстом не проходит. Это поле используется при проверке, в HTTP r1 не добавляется. Смысл функции и выводов по-прежнему требует содержательной проверки.
- Расход полученного ответа учитывается до разбора JSON, поэтому ошибка формата больше не теряет известный usage. После сетевого таймаута полного расхода по-прежнему может не быть.

## Воспроизведённые проверки

- Backend: 76 автоматических проверок, все прошли. Сетевых запросов нет.
- Frontend: 18 браузерных проверок объединённой версии прошли (Chrome), включая мобильный экран, axe и новую навигацию товарища.
- Настоящий браузер на production preview :4173 → FastAPI :8000 → OpenAI → отчёт → открытие цитат: выполнено на синтетическом комплекте.
- Повтор HTTP POST с прежним ключом/файлами вернул тот же завершённый анализ без изменения расхода модели.

## Контрольный синтетический комплект (Luna / none)

Итоговый проверенный запуск: `f434f77a0d234cf280181187f32166c7`, gpt-6-luna, 32.477 с от нажатия до отчёта и открытого источника, 4 вызова API, 15 880 входных / 3 239 выходных токенов, оценка $0.0032075. Кэш-скидки не вычитаются; это расчёт, не выписка биллинга.

Все пять размеченных случаев проверены по настоящему отчёту: переименование без ложной потери; архивная функция как возможная потеря с поиском по after; дубль реестра; конфликт выполнения/контроля платежей; подготовка и утверждение плана без ложного дублирования. Получены 15 функций, 8 соответствий и 3 замечания. Независимая проверка всех 15 источников функций, 6 строк подразделений и 8 сопоставлений не обнаружила ошибочных ссылок. expected.json модели не передавался. Повтор HTTP POST с теми же файлами/ключом вернул прежний отчёт без изменения usage.

Ранее выполненный `11d1a4556f6a45beb8c47104146b4de1` дал ожидаемые выводы ($0.0033539), но независимая проверка обнаружила четыре неверные ссылки «до» в строках подразделений. Он не засчитывается как полное прохождение проверки цитат.

Предыдущие прогоны сохранены локально: один отклонён из-за неверной ссылки ($0.0036653), второй завершён, но заменил потенциальную потерю на недостаточность данных без нужного поиска ($0.0033289). Они стали основаниями для исправлений, не засчитываются как полное прохождение.

Прогон `daeccd8d86c04300a8cf22661aa0961c` также отклонён из-за неизвестного адреса цитаты после коррекции (5 запросов, 23 229 входных / 8 019 выходных токенов, $0.0063324). После этого адреса ограничены списком в схеме генерации; обычный серверный валидатор сохранён.

`fa91c42e93eb4740978972f5b30e160b` остановлен ошибкой разбора ответа SDK: 5 начатых запросов, usage получен только для первых четырёх (17 262 входных / 2 894 выходных токена), полный расход неизвестен. Учёт usage теперь выполняется до разбора ответа. `0a72ab1384dc4c7aae05e1d3a2fdf32b` завершился ($0.0040215), но содержательная проверка выявила неверные before-ссылки всех семи функций: действительные ID указывали на соседние абзацы. Поэтому добавлены непосредственная связь ID/quote во входе и дословный source_excerpt. Эти прогоны не засчитываются как прохождение.

## Две редакции положения о внутреннем аудите (Luna / none)

Пользовательские DOCX хранятся только локально. Извлечение: 491 + 490 фрагментов, 167 255 символов исходного текста вместо 514 тысяч символов цитат с повторяющимся соседним контекстом. Служебные поля источников, инструкции и схема считаются отдельно.

Итоговый запуск `e65159e0e6e64a6e9cef57bcaece23bf` завершён через браузер: 332.528 с, 5 запросов (включая одну коррекцию), 386 880 входных / 39 380 выходных токенов, оценка $0.058378. Получены 70 функций до и 70 после, 70 сопоставлений (54 preserved, 15 unresolved, 1 changed), 4 insufficient_evidence. Отчёт и источник открываются в интерфейсе. Технический сценарий работает; содержательная полнота не достигнута.

Независимая выборочная проверка по шести изменениям, выбранным до получения модельного отчёта:

| Контрольное изменение | Результат |
| --- | --- |
| Новые ДИТААД и ДОА | Отражено хорошо, источники структуры корректны |
| Изменение исполнителей функций ДНМ | Частично: функции найдены, связь оставлена unresolved |
| Расширение мониторинга корректирующих мер | Частично: функции найдены, изменение не раскрыто полностью |
| Сохранение качества/методологии/обучения ДККМ | Отражено хорошо |
| §9.15: обязательное действие → «может осуществляться» | Пропущено |
| §9.37: изменение адресата делегирования | Пропущено |

Реестр главным образом охватывает §2–5.5; обязанности поздних §9–12 представлены неполно. Дополнительные замечания проверки: m65 ошибочно называет обязанность правом; f3 слишком широко заявляет об отсутствии периодичности отчётности в новой редакции. Периодичность убрана из отдельного пункта ДККМ, но общий квартальный/годовой цикл сохранился в §5.1.6, к требованиям Положения есть отсылка. Такой вывод требует уточнения сотрудником. Реальные отчёты вручную не исправлялись и не выдавались за более точный результат модели.

В ТЗ нет численного проходного порога и требования 100% обнаружения: раздел 11 проверяет несколько известных случаев и их источники. Пользователь уточнил, что приоритет — измеримый MVP и честное описание ограничений. Новая обработка по частям отложена; указанные результаты не являются общим процентом точности или официальным баллом.

Первый живой прогон `64e0af9235ce4b2a9e44845699056017` отклонён: в сопоставлениях недоставало источников связанных функций, модельная коррекция не решила проблему. 5 запросов, 254 035 входных / 37 359 выходных токенов, оценка $0.044083. Непроверенный отчёт пользователю не выдан. Это основание для явного наследования источников по связям. Шесть качественных проверок выше — выборка, не полный эталон всех обязанностей.

Прогон `5acaa8a1bc114d71b460bd1036856550` остановлен по прежнему таймауту одного ответа 240 с. Начаты 4 запроса; usage есть только для первых трёх (131 918 входных / 114 выходных токенов), итоговая стоимость неизвестна. Не считать этот расход нулевым. Лимит одного ответа увеличен до 480 с внутри прежних 600 с всего анализа; формулировки отчёта сделаны компактнее без сокращения исходного текста.

`c05539b828b0447c83acc6ab2049232a` отклонён по неверным цитатам подразделений после коррекции (5 запросов, 266 401 входной / 36 866 выходных токенов, $0.0450731). Он использовал прежнее разделение texts/индексов; текущая версия связывает ID непосредственно с текстом.

Ограничения: наличие корректных source ID не доказывает истинность выводов и полноту реестра. OCR и старые DOC/XLS не поддерживаются. Публичный многопользовательский запуск не предусмотрен. Независимый запуск товарищем по общей инструкции ещё предстоит.

## Сравнение с Luna / medium

По указанию пользователя добавлена настройка OPENAI_REASONING_EFFORT (по умолчанию medium; прежний none доступен для сравнения). Парсер, исходные документы, инструкции анализа и HTTP r1 сохранены. Промежуточному ответу выделено до 8192 выходных токенов вместо 2048, чтобы reasoning и вызов инструмента помещались в один ответ. Общие пределы 5 запросов, 600 секунд и оценочный бюджет $0.25 сохранены. Backend: 80 тестов прошли, включая передачу режима, ранний отказ неверной настройки и сохранение reasoning-контекста между вызовами инструментов.

Запуск medium `58b685899aae46c5b8f5459d01b0cbcc` завершён через тот же production preview: 200.947 с, 5 запросов, 363 521 входной / 21 295 выходных токенов, оценка $0.0469996. Fingerprint исходных файлов и все 981 извлечённый источник побайтно/структурно совпадают с baseline e651. Получены 42 функции, 21 сопоставление preserved, 1 insufficient_evidence. Все 42 ссылки функций соответствуют источникам, однако функции относятся только к §2.4. Это не более компактное представление прежнего полного набора: обязанности остальных разделов не включены, что модель прямо признаёт в limitations.

| Показатель одного прогона | Luna / none | Luna / medium |
| --- | --- | --- |
| Функций / сопоставлений | 140 / 70 | 42 / 21 |
| Проверка шести выбранных изменений | 2 хорошо, 2 частично, 2 пропущены | Новые подразделения отражены; остальные 5 изменений не отражены |
| Время | 332.528 с | 200.947 с |
| Оценка расхода | $0.058378 | $0.0469996 |

Прежние неточности f3/m65 в medium не повторены, но соответствующие обязанности вообще исключены из анализа; это не доказательство исправленного понимания. Более узкий результат и явное ограничение охвата подтверждены независимым чтением отчёта. Улучшение качества medium на этой паре не подтверждено; одиночный прогон не устанавливает универсальное превосходство одного режима. Следующая гипотеза — контролируемый обход разделов/сопоставление изменённых пунктов при сохранении Luna; такой механизм сейчас не добавлен. Эталонные ответы модели не передавались.

В пропущенных ранее §9.15 и §9.37 текст успешно извлечён парсером и передан модели: проблема выявлена на этапе выделения функций и сопоставления смысла, а не в чтении этих абзацев DOCX. Более сложный парсер может понадобиться для сканов/сложной вёрстки, но не считается исправлением обнаруженных смысловых пропусков без отдельной проверки.

Варианты для будущей проверки чтения: [python-docx](https://github.com/python-openxml/python-docx) уже используется для DOCX; [Docling](https://github.com/docling-project/docling) поддерживает структуру PDF, таблицы и OCR; [MarkItDown](https://github.com/microsoft/markitdown) преобразует документы в Markdown. Новые парсеры в этой проверке не устанавливались.

После живого сравнения подтянут коммит товарища 225a4ef с переходами и загрузкой источников. Production build успешен; 22 браузерные проверки Chrome прошли за 31.8 с. Это регрессия UI с контролируемыми ответами, не дополнительный платный модельный прогон.


## Контроль обзора различий — эксперимент после medium baseline

Пользователь задал цель не менее пяти из шести прежних контрольных случаев на своей паре документов. Это локальный ориентир качества, не официальный процент из ТЗ и не оценка точности на других документах. NVIDIA отложена по прямому указанию пользователя; модель остаётся Luna / medium.

В прежнем варианте полный текст успешно поступал модели, но один общий проход позволял сосредоточиться на начале документа. Новый проход сначала получает механически найденные блоки различий (SequenceMatcher, без удаления повторяющихся абзацев). Он отдельно описывает изменения, сохранённые обязанности в изменённых блоках, неопределённости и редакционные правки. Сопоставление по смыслу остаётся работой модели по всему исходному комплекту; механический блок не объявляется доказанным соответствием. Эталонные ответы и выбранные для оценки номера пунктов не добавляются во вход модели.

Промежуточные неудачи сохранены в истории:

- `739811595b43489bac829c6693c1dd0c`: обзор не прошёл проверку блоков/источников, итоговый отчёт не создан. 124.712 с до записи ошибки, один запрос, 69 820 входных / 15 106 выходных токенов, $0.014535. После этого список блоков заменён обязательными ключами модельной схемы, адреса ограничены существующими источниками.
- `afbc6e6b6e3c4ccc9a8ce0684072a3c9`: обзор отклонён проверкой происхождения источников, отчёт не создан. 114.639 с до записи ошибки, один запрос, 77 356 входных / 10 932 выходных токена, $0.0132016. При разборе обнаружено чрезмерное правило: каждая заметка должна была касаться изменённого абзаца. Исправлено — блок в целом должен ссылаться на изменённую область, а отдельные сохранённые/перенесённые функции могут ссылаться на неизменённые соседние абзацы или другие разделы. Для этого добавлен отдельный тест. Промежуточные ответы этих двух запусков не сохранялись, поэтому конкретную ошибочную заметку задним числом не восстанавливаем.

Подтянуты коммиты товарища `7c69e70` и `b2b162a` (название кнопки скачивания и документация frontend). Production build и все 22 браузерных теста Chrome прошли; собственных изменений оформления нет.


Третья попытка `2769f34fb72b40a78d8477a7c369d1f3` прошла обзор: 37 блоков, 73 заметки. Черновик итогового отчёта независимо оценён как 4/6: перенос координации, сохранение трёх функций ДККМ, изменение обязательности анализа и делегата отражены; распределение областей ДИТААД/ДОА лишь частично, расширенный мониторинг корректирующих мер пропущен. Финальный исправленный ответ отклонён из-за source_excerpt двух функций конфиденциальной информации. Запуск не опубликован: 292.126 с, 6 запросов, 499 103 входных / 35 884 выходных токена, $0.0678523.

Для следующей попытки сборка отчёта разделена на две группы блоков; код объединяет ID и сохраняет явные ссылки. Копирование source_excerpt отдельно требует исходной грамматической формы цитаты. Контроль наличия источников обзора теперь сообщает ограничения охвата: отсутствие источника в итоговой матрице не тождественно неверной цитате и не должно само по себе отклонять частичный полезный отчёт. Некорректные ID, версии и дословные фрагменты по-прежнему отклоняются. Бюджет — до 6 запросов, 600 секунд и $0.25; максимум одна коррекция всего анализа.

**Передача текущей версии по запросу пользователя:** 91 backend-тест прошёл. Прогон `df080eafe37d4790a2267dc2eeb68460` находится в работе; достижение ≥5/6 пока не подтверждено. Товарищ параллельно занимается безопасностью, графикой и независимым запуском.


`df080eafe37d4790a2267dc2eeb68460` не завершён: 337.640 с, 5 запросов, 416 391 входной / 44 122 выходных токена, $0.0637001. Первая часть содержала 104 функции; исправление целого отчёта ради одной составной цитаты не устранило другой форматный конфликт: модель также возвращала строки новых/несопоставленных функций only-after. Публичная схема допускает пустой список before, UI уже умеет отображать такую строку, но сервер её отклонял. Валидатор уточнён: changed/unresolved допускают только-after; preserved/transferred требуют before; оба списка пустыми быть не могут. Наличие только-after не доказывает новизну функции в организации.

Дословные фрагменты теперь проверяются после объединения частей, и при необходимости один ограниченный запрос исправляет только неверные source_excerpt по исходным цитатам. Функции и смысловые выводы при такой коррекции не переписываются. Прочие структурные ошибки по-прежнему проверяются; общий лимит одной коррекции сохранён. 96 backend-тестов прошли, включая after-only и неизменность отчёта при точечном исправлении цитаты. Новый прогон `d82e862c958e4369b258794442f98148` выполняется; качество ≥5/6 пока не заявляется.


`d82e862c958e4369b258794442f98148`: 357.091 с, 5 запросов, 409 200 входных / 40 945 выходных токенов, $0.0613925. Независимая проверка черновика первой части подтвердила первые четыре контрольных случая, включая области ДИТААД/ДОА и расширение мониторинга. Итоговый запуск всё же отклонён: ссылки строк подразделений опирались на обязанности и сокращения без полного названия из перечня структуры. Промежуточный черновик не выдаётся за опубликованный отчёт.

Добавлено детерминированное присоединение абзаца с дословным полным названием подразделения из той же версии. Исходные модельные ссылки сохраняются, неизвестные ID не удаляются; функция не устанавливает правопреемство или смысловую истинность остальных выводов. 97 backend-тестов прошли. Следующий пользовательский запуск `d3c1da1a66204b6dbbe8bf9c5d089248` и авторская регрессия `94883f76f6d4428f859613f129697a0c` пока в работе.
