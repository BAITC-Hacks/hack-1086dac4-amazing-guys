# Проверка общей версии — 2026-09-23

Интерфейс «Контур» из коммита `25e4184` проверен вместе с FastAPI. Визуальный стиль сохранён; изменение frontend касается только предела ожидания (10 минут).

## Исправления

- Большие документы: лимит считается по исходному тексту (250 000 символов), а не по многократно скопированному контексту. Точные совпадения текстов хранятся один раз в запросе; все source ID, принадлежность документам и порядок сохраняются. Ни один фрагмент не отбрасывается. Полный соседний контекст доступен через read_evidence и API источников.
- Локальные порты 5173/4173/3000 разрешены для localhost и 127.0.0.1. CORS_ORIGINS добавляет дополнительные адреса; ключ остаётся только на backend.
- OpenAPI описывает фактический error-конверт вместо стандартного detail, а также health и HTTP-коды ошибок.
- Отчёт с некорректными ссылками получает не более одной модельной коррекции и повторную проверку; если она неуспешна, отчёт не публикуется. Максимум 5 запросов модели и оценочный бюджет $0.25 на анализ.
- Первое действие модели — поиск по комплекту после для проверки назначения функции с неочевидным правопреемником. Последующие инструменты выбирает модель.
- Колонтитул только с полем номера страницы не помечает весь документ как частично непрочитанный. Содержательный пропущенный текст по-прежнему вызывает предупреждение.

## Воспроизведённые проверки

- Backend: 69 автоматических проверок, все прошли. Сетевых запросов нет.
- Frontend: production build успешен; 12 браузерных проверок прошли (Chrome), включая мобильный экран и axe.
- Настоящий браузер на production preview :4173 → FastAPI :8000 → OpenAI → отчёт → открытие цитат: выполнено на синтетическом комплекте.
- Повтор HTTP POST с прежним ключом/файлами вернул тот же завершённый анализ без изменения расхода модели.

## Контрольный синтетический комплект

Проверенный запуск: `11d1a4556f6a45beb8c47104146b4de1`, gpt-6-luna, 38.1 с от нажатия до отчёта и открытого источника, 4 вызова API, 16 949 входных / 3 318 выходных токенов, оценка $0.0033539. Кэш-скидки не вычитаются; это расчёт, не выписка биллинга.

Все пять размеченных случаев проверены по настоящему отчёту: переименование без ложной потери; архивная функция как возможная потеря с поиском по after; дубль реестра; конфликт выполнения/контроля платежей; подготовка и утверждение плана без ложного дублирования. Получены 15 функций, 7 соответствий и 3 замечания. expected.json модели не передавался.

Предыдущие прогоны сохранены локально: один отклонён из-за неверной ссылки ($0.0036653), второй завершён, но заменил потенциальную потерю на недостаточность данных без нужного поиска ($0.0033289). Они стали основаниями для исправлений, не засчитываются как полное прохождение.

## Две редакции положения о внутреннем аудите

Пользовательские DOCX хранятся только локально. Извлечение: 491 + 490 фрагментов, 167 255 символов исходного текста. После упаковки запрос источников занимает около 145 тысяч символов вместо 514 тысяч символов цитат с повторяющимся контекстом (служебные инструкции и схема считаются отдельно).

Живой прогон больших документов выполняется; его результат ещё не засчитан как успешный. Независимо от модели подготовлены шесть качественных проверок изменений. Это выборочная проверка, не полный эталон всех обязанностей.

Ограничения: наличие корректных source ID не доказывает истинность выводов и полноту реестра. OCR и старые DOC/XLS не поддерживаются. Публичный многопользовательский запуск не предусмотрен. Независимый запуск товарищем по общей инструкции ещё предстоит.
