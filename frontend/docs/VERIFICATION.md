# Проверка frontend — 2026-09-23

## Индикаторы ожидания

- `npm run build`: успешно; `PLAYWRIGHT_CHANNEL=chrome npm test`: **28 passed**, 38,1 секунды.
- Подготовка двух комплектов блокирует преждевременный запуск и не отправляет файлы в сеть.
  После подготовки доступно сравнение; удаление последнего файла снова блокирует запуск.
- Контролируемая HTTP-проверка: ожидание отправки и отчёта, отсутствие преждевременного
  результата, отмена при получении отчёта и повтор без второго POST анализа.
- Быстрые нажатия двух кнопок экспорта дают один полный JSON; обе кнопки показывают ожидание.
- Все прежние сценарии, включая гостевой вход, источники, ошибки и повтор запросов, проходят.
- Проверены снимки подготовки файлов на компьютере и обработки на экране 390×844.
  Переполнения по ширине нет; axe на окне обработки не обнаружил нарушений WCAG 2 A/AA и 2.1 AA.
  В reduced motion текст ожидания сохраняется, вращение отключается.
- Серверные ответы подменены локальными фикстурами; живой backend и модель не вызывались.

## Обновление Kontur и гостевого входа

- Название продукта: Kontur; подпись и название вкладки: organizational intelligence.
- `npm run build`: успешно после добавления гостевого входа.
- Все 22 существующих сценария прошли при общем прогоне.
- После исправления начального фокуса повторно выполнен `tests/guest.spec.ts`: **3 passed**, 5,8 секунды.
  Проверены загрузка и отмена входа, сохранение выбранных файлов, отсутствие запросов API,
  восстановление гостевого флага после обновления, Escape поверх источников,
  недоступный sessionStorage, мобильный экран, reduced motion и axe.
- Вручную просмотрены окно входа на компьютере и телефоне; после входа показан «Гость»,
  горизонтального переполнения нет. Гостевой флаг не является серверной авторизацией.
- Снимки: [окно входа](guest-entry.png), [на телефоне](guest-entry-mobile.png).

## Предыдущая проверка навигации и анимаций

- `npm run build`: успешно (TypeScript и production-сборка Vite).
- `PLAYWRIGHT_CHANNEL=chrome npm test`: **22 passed**, 28.7 секунды после совмещения с изменениями команды.
- Проверено прямое открытие каждого из пяти разделов с первого посещения без API-запросов;
  в режиме сервера без отчёта показано объяснение и явный переход к демопримеру.
- Проверены основной демосценарий, точность всех цитат, таблицы и фильтры,
  сохранение JSON, валидация файлов, неполное покрытие, сбой, пустой результат.
- HTTP-интеграция проверена на контролируемых ответах Playwright по r1:
  multipart, polling, URL источника, ошибочный формат, сетевой повтор с тем же
  ключом и новый ключ после фонового failed, безопасный вывод текста.
- axe: стартовый экран, обзор, документы, замечания, заключение и сравнение
  с открытым источником — без обнаруженных нарушений WCAG 2 A/AA и 2.1 AA.
  Автоматический аудит не заменяет полноценную ручную проверку доступности.
- Мобильный сценарий 390×844: меню, таблица, панель источников, отсутствие
  горизонтального переполнения страницы. Таблица имеет собственную прокрутку.
- Вручную просмотрены реальные снимки desktop и mobile; консоль браузера без ошибок.
- Обновление анимаций: короткие переходы разделов и вкладок, открытие и закрытие
  панели, подсветка текущей записи. Без новых зависимостей.
- Контролируемый медленный HTTP-ответ: скелетоны сменяются точными цитатами;
  ошибка убирает загрузку, повтор восстанавливает источники. Реальный сервер не вызывался.
- Проверены отмена устаревшего ответа и незавершённого закрытия при смене записи,
  возврат фокуса к последней записи, Tab/Shift+Tab на телефоне и отключение
  CSS/JS-анимаций при reduced motion. axe запускается после завершения переходов.

## Снимки работающего приложения

- [Обзор](overview.png)
- [Сравнение и источники](comparison.png)
- [Источник на телефоне](mobile-evidence.png)
- [Скелетоны источников: HTTP-подмена Playwright, не живой backend](source-loading.png)

## Что не проверялось

При проверке этого обновления живой backend и модель не запускались.
Отдельные результаты интеграции команды — в [INTEGRATION_RESULTS.md](../../docs/INTEGRATION_RESULTS.md).
PDF/DOCX/XLSX не анализируются браузером.
Семантическое качество AI и извлечение реальных документов проверяет backend-команда.
Лимиты расходов и ключи модели относятся к серверу. Публичный сайт не развёрнут;
проверен локальный frontend. Тесты не отправляют документы во внешние сервисы.
